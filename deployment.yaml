apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-bot-kde-v3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloud-bot-kde-v3
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: cloud-bot-kde-v3
    spec:
      containers:
      - name: cloud-bot-kde-v3
        image: gcr.io/PROJECT_ID/IMAGE:TAG
        ports:
        - containerPort: 8443
        env:
          - name: PORT
            value: "8443"
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m