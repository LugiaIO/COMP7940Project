apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-bot-kde-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloud-bot-kde-v2
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: cloud-bot-kde-v2
    spec:
      containers:
      - name: cloud-bot-kde-v2
        image: gcr.io/PROJECT_ID/IMAGE:TAG
        ports:
        - containerPort: 8443
        env:
          - name: PORT
            value: "8443"
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m