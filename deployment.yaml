apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-bot-kde-v7
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloud-bot-kde-v7
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: cloud-bot-kde-v7
    spec:
      containers:
      - name: cloud-bot-kde-v7
        image: gcr.io/PROJECT_ID/IMAGE:TAG
        ports:
        - containerPort: 8080
          protocol: TCP
        env:
          - name: PORT
            value: "8443"
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m